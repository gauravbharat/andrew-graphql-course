const Comment = {
  // author(parent, args, ctx, info) {
  //   return ctx.db.populateAuthor(parent, args, ctx, info);
  // },
  // post(parent, args, { db }, info) {
  //   return db.posts.find((post) => {
  //     return post.id === parent.post;
  //   });
  // },
};

module.exports = Comment;
// export default Comment;
